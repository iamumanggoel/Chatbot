{
  "builds": [
    {
      "src": "index.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(?!.*\\.(js|css|svg|jpg|png|ico|json)$).*", "dest": "index.py" }
  ]
}
